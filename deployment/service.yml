apiVersion: v1
kind: Service
metadata:
  name: kava-d-service2
  labels:
    app: kava
spec:
  type: LoadBalancer
  ports:
    - name: rpc
      port: 46657
      protocol: TCP
      targetPort: 46657
    - name: p2p
      port: 46656
      protocol: TCP
      targetPort: 46656
  selector:
    pod: kvd
---
apiVersion: v1
kind: Service
metadata:
  name: kava-lcd-service2
  labels:
    app: kava
spec:
  type: LoadBalancer
  ports:
    - name: rest-server
      port: 1317
      protocol: TCP
      targetPort: 1317
  selector:
    pod: kvcli-lcd
